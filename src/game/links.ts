/** External project links, kept in one place so deployments can rebrand easily. */
export const DONATE_URL = 'https://github.com/sponsors/Zeecka'

/** The source repo, as `owner/name` — used for the GitHub link and star count. */
export const GITHUB_REPO = 'Zeecka/TmuxLegends'
export const GITHUB_URL = `https://github.com/${GITHUB_REPO}`

/**
 * The sibling game, cross-promoted with a footer button. TmuxLegends teaches tmux;
 * its companion VimLegends teaches Vim with the same engine and design system.
 * `SISTER_ACCENT` is the sibling's primary accent hue (its `--color-term`) so the
 * button visually evokes the destination brand rather than this app's own accent.
 */
export const SISTER_URL = 'https://vimlegends.com'
export const SISTER_NAME = 'VimLegends'
export const SISTER_ACCENT = '#7c6bff' // vim electric violet
